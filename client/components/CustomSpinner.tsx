import { Center, Spinner } from "@chakra-ui/react";

const CustomSpinner = () => {
  return (
    <Center w="full" h="85vh">
      <Spinner size="xl" />
    </Center>
  );
};

export default CustomSpinner;
